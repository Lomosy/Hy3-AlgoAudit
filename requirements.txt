# ============================================================
#  Hy3-TraceJudge 依赖清单
# ============================================================

# Hy3 模型调用（OpenAI 兼容接口）
openai>=1.0

# 环境变量加载（读取 .env）
python-dotenv>=1.0
